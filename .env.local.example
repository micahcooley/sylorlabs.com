# Resend API Key
# Get your API key from: https://resend.com/api-keys
RESEND_API_KEY=re_your_api_key_here

# Authentication Configuration
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Base URL Configuration (Optional)
# If not set, the app will construct the URL from NEXT_PUBLIC_PROTOCOL, NEXT_PUBLIC_HOST, and NEXT_PUBLIC_PORT
# For production, set this to your full domain (e.g., https://sylorlabs.com)
NEXT_PUBLIC_BASE_URL=

# URL Construction Settings (Optional - used when NEXT_PUBLIC_BASE_URL is not set)
# These allow flexible configuration for different environments without hardcoded ports
NEXT_PUBLIC_PROTOCOL=http
NEXT_PUBLIC_HOST=localhost
NEXT_PUBLIC_PORT=3000
# Note: You can also use the PORT environment variable which Next.js recognizes

# Google OAuth Configuration
# Get credentials from: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
# If not set, will be constructed from NEXT_PUBLIC_BASE_URL or the URL construction settings
GOOGLE_REDIRECT_URI=
